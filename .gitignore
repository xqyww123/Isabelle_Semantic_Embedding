# VIM
*.ll
*~
*.swp
*.swo
.*.marks
*.phi-cache

\#*
.*#

__pycache__
doc/.obsidian

dist
IsaREPL.egg-info
.idea
.trash
build
# Highway's out-of-tree build. Not `build`: Highway ships a Bazel file named BUILD,
# and macOS filesystems are case-insensitive, so the two would collide there.
hwybuild
*.egg-info
secret.sh

# `pip install -e .` builds the _vecgather extension module in place.
Isabelle_Semantic_Embedding/_vecgather*.so
Isabelle_Semantic_Embedding/_vecgather*.pyd

*.proof-store.lock

# Archived measurement data (TSV/JSON dumps from the infra-filter and padding runs).
archive/data/

# one-off jEdit experiments and a probe moved out of Test/ on 2026-09-08; never tracked again
ai-artifacts/Document_State_Experiment.thy
ai-artifacts/Eval_Thy_Experiment.thy
ai-artifacts/Probe_Command_Header_Test.thy
